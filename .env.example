# Copy to .env and fill in your key. NEVER commit .env.
# Backend-level config lives in backend/.env (see backend/.env.example);
# the Avatar service has its own Avatar/.env (see Avatar/.env.example).
OPENAI_API_KEY=sk-...
